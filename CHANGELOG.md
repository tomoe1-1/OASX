# 更新日志

本仓库为 `xylolit-mu/OASX`（上游，fork 自 `AzurTian/OASX`）Windows 发布产物的同步镜像，
按上游版本号逐版同步，不改动任何二进制内容。

## v0.3.14

同步自上游 Windows 发布包 `oasx_v0.3.14_windows.zip`，27 个文件全部 SHA256 一致。

### 本次同步解决的直接问题

- **配置参数新增 `multi_enum` 多选下拉类型**（上游 v0.3.13 引入）
  此前本仓库产物停留在 v0.3.12，参数类型只支持
  `boolean / string / multi_line / number / integer / enum / date_time / time_delta / time`，
  **缺少 `multi_enum`**，导致活动中此类参数无法选择。本次同步后已补齐。

### 同时带来的上游改动

**新增**

- 日志行为分析页：支持点击位置、滑动轨迹与点击顺序连线
- 每 4 小时无意义点击与总点击密度、近几日每日趋势、任务点击强度排名图表
- 批量立即执行 & 等待任务功能

**修复**

- 修复日志分析请求超过服务端字节上限返回 422 的问题
- 日志分析加载失败时显示实际错误信息，不再误报为无可用日志
- 修复日志接口压缩时间戳后无法识别操作记录、历史翻页提前停止的问题
- 多选下拉菜单高度约束

**改进**

- 缩小点击轨迹点与顺序连线，降低轨迹颜色强度
- 部署过程出现异常不再停止
- 退出弹窗选项优化为互斥态，再次点击即可取消选择
- 配置项支持导入 / 脱敏导出，任务详情支持导入导出与复制
- 安装和启动 OAS 可使用自定义 Python
- 部署时拉取仓库实时显示拉取大小与进度
- 错误日志列表为空时的提示信息优化

## 产物校验

同步后的仓库与上游发布包逐文件比对结果：

| 文件 | 说明 |
|---|---|
| `data/app.so` | 9,847,728 → 10,208,176 字节（核心逻辑，含 `multi_enum`） |
| `oasx.exe` | 已更新 |
| `flutter_windows.dll` | 已更新 |
| 9 个 `*_plugin.dll` | 已更新 |
| `data/flutter_assets/AssetManifest.{bin,json}` | 已更新 |
| 字体 / 图标 / shader / `icudtl.dat` | 内容一致，未变动 |

校验方式：对上游发布包内全部 27 个文件逐一计算 SHA256，与仓库内同名文件比对，全部一致。
