# 更新日志

本仓库（`tomoe1-1/OASX`）同时托管 **OASX 桌面端 Windows 构建产物**（仓库根目录）
与**完整 Flutter 构建源码**（[`src/`](src/)）。

自 **v1.0.0** 起不再跟随上游逐版镜像，而是发布本分支自行构建的成品 ——
应用内的「检查更新」即指向本仓库的 releases，因此上游发版不会覆盖本地改动。

## v1.1.2

新增 OAS 脚本自动修复的可视开关。源码位于同仓库 [`src/`](src/)，构建命令 `flutter build windows --release`。

- 构建工具链：Flutter 3.47.5 stable / Dart 3.13.4
- 产物：`oasx.exe` + `data/app.so` + `data/flutter_assets/` + 各插件 DLL

### 本次改动

**新增**

- 「主目录 → 脚本」列表下方新增两个开关，仅在 OAS 提供对应接口时显示：
  - **智能诊断与脚本修复** —— 启用本地证据采集、代码异常修复与每日活动检查
  - **允许发送游戏证据给 Codex** —— 单独授权把遮盖常见账号区域的截图及 OCR 文字发送出去用于分析
- 两个开关均默认关闭；保存失败时提示「请检查 OAS 服务」，不静默失败

**接口对接**

- 读写 OAS 的 `/tool/repair/status`、`/tool/repair/enabled`、`/tool/repair/evidence/enabled`
- 设置由 OAS 侧持久化在 `deploy/auto_repair.json`，本客户端不保存任何副本
- OAS 未提供该接口时整块区域不渲染，老版本 OAS 的界面不受影响

**版本**

- 应用版本由 v1.0.0 提升至 v1.1.2

### 校验

- `oasx.exe` 版本资源：`FileVersion` / `ProductVersion` = `1.1.2+12`
- 产物 104448 字节，与 v1.0.0 一致（仅版本资源变化）
- 本次版本资源由重新链接 C++ runner 写入；Dart AOT 产物（`data/app.so`）沿用 17:52 的构建，
  其中两个版本兜底常量仍为旧值 —— 发布模式下界面版本号取自 exe 资源，不受影响


## v1.0.0

首个自构建正式版。源码位于同仓库 [`src/`](src/)，构建命令 `flutter build windows --release`。

- 构建工具链：Flutter 3.47.5 stable / Dart 3.13.4
- 产物：`oasx.exe` + `data/app.so` + `data/flutter_assets/` + 各插件 DLL

### 本次改动

**界面重做**

- 新增全局设计令牌层（间距 / 圆角 / 动效 / 字体层级 / 语义色 / 表面样式），
  清除散落的硬编码数值：159 处间距值 + 4 处圆角 + 10 处动画时长全部收口到令牌
- 重做主工作台布局：状态头栏 + 分段页签 + 内容切换动画
- 新增 9 种配色主题（基础紫、靛蓝、天蓝、青碧、森绿、暖黄、活力橙、朱砂、樱粉），
  设置页可实时切换并持久化

**可访问性**

- 语义色改为亮/暗双档，修掉 6 处 WCAG 对比度不达标（最低 2.15:1 → 全部 ≥ 4.5:1）
- 界面内全部动画时长改经 `Motion.of` 取值，尊重系统「减少动态效果」偏好
- 日志级别增加非颜色区分手段（前缀符号 + 字重），色盲与灰度场景可辨
- 补全 10 处图标按钮 `tooltip`，装饰性图标标记为语义排除
- 关键状态指示灯补充读屏标注，命中区放大到 40px 以上

**性能**

- 日志列表（运行中每 120ms 刷新）与信息日志列表逐行加 `RepaintBoundary`，
  把重绘限制在真正变化的行上

**修复**

- 修复深色主题下表格分隔线与部分文字对比度偏低的问题
- 修复状态灯、配色圆点等纯装饰元素被读屏重复播报的问题

**仓库**

- 源码并入本仓库 [`src/`](src/)（git 子树合并，保留上游完整提交脉络）；
  此前源码位于独立分支 `self`，需另行获取
- 源码侧移出误入库的 `android/build/reports/`（Gradle 生成的报告，非源码），
  并补全各平台构建产物的忽略规则

**功能**

- 配置参数支持 `multi_enum` 多选下拉类型

### 校验

- `flutter analyze`：0 error（仅 3 条既有 `unawaited_return_in_try_block` warning）
- 烟测：启动后正常创建主窗口并稳定运行，Impeller 渲染后端初始化成功


## v0.3.14（上游镜像，历史记录）

同步自上游 Windows 发布包 `oasx_v0.3.14_windows.zip`，27 个文件全部 SHA256 一致。

### 该次同步解决的直接问题

- **配置参数新增 `multi_enum` 多选下拉类型**（上游 v0.3.13 引入）
  此前本仓库产物停留在 v0.3.12，参数类型只支持
  `boolean / string / multi_line / number / integer / enum / date_time / time_delta / time`，
  **缺少 `multi_enum`**，导致活动中此类参数无法选择。该次同步后已补齐。

### 同时带来的上游改动

**新增**

- 日志行为分析页：支持点击位置、滑动轨迹与点击顺序连线
- 每 4 小时无意义点击与总点击密度、近几日每日趋势、任务点击强度排名图表
- 批量立即执行 & 等待任务功能

**修复**

- 修复日志分析请求超过服务端字节上限返回 422 的问题
- 日志分析加载失败时显示实际错误信息，不再误报为无可用日志
- 修复日志接口压缩时间戳后无法识别操作记录、历史翻页提前停止的问题
- 多选下拉菜单高度约束

**改进**

- 缩小点击轨迹点与顺序连线，降低轨迹颜色强度
- 部署过程出现异常不再停止
- 退出弹窗选项优化为互斥态，再次点击即可取消选择
- 配置项支持导入 / 脱敏导出，任务详情支持导入导出与复制
- 安装和启动 OAS 可使用自定义 Python
- 部署时拉取仓库实时显示拉取大小与进度
- 错误日志列表为空时的提示信息优化
